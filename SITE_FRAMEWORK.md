# AI Agent 轻量维护框架

本文档说明本站点内部给 AI Agent 使用的轻量生成框架。它不是 Astro、Vue 或 React，也不会改变部署方式；站点最终仍然是纯 `HTML/CSS/JS`，Cloudflare Pages/Worker 仍然不需要构建命令。

## 目标

- 把重复性强的内容页维护从“手写整页 HTML”改为“维护结构化源数据，再生成 HTML”。
- 降低新会话或其它 AI Agent 接手时的阅读量。
- 让页面数量、朗读按钮、词汇列、路径规则等常见错误能被脚本检查出来。
- 保持现有页面路径、视觉样式和前端交互不变。

## 第一阶段范围

当前只纳入英语单词词汇页：

- `content/english/vocab/*.json` 是英语单词页源数据。
- `tools/build_vocab_page.py` 根据源数据生成对应 `subjects/english/.../words/index.html`。
- `tools/build_indexes.py` 根据 `site-manifest.json` 生成英语二级清单页。
- `tools/validate_site.py` 做站点结构和词汇页基础校验。

英语课文页和 Little Fox 故事页暂时仍按现有 skill / builder / 手工维护方式处理。不要在第一阶段强行迁移它们。

## 目录约定

```text
primary-knowledge-site/
  content/
    english/
      vocab/
        grade-3-second.json
        grade-4-first.json
        grade-5-second.json
  tools/
    build_vocab_page.py
    build_indexes.py
    validate_site.py
    import_vocab_from_html.py
  site-manifest.json
```

## 源数据优先

已纳入框架的英语单词页，源数据是 `content/english/vocab/*.json`，生成出来的 HTML 是输出结果。

维护规则：

- 新增或修改英语单词页时，优先修改对应 JSON。
- 运行 `python tools/build_vocab_page.py --all` 重新生成 HTML。
- 如新增页面入口，更新 `site-manifest.json` 后运行 `python tools/build_indexes.py`。
- 不要直接长期手改由框架生成的词汇页 HTML；如果临时手改，必须同步回 JSON。

生成页会包含类似下面的注释：

```html
<!-- Generated from content/english/vocab/grade-4-first.json by tools/build_vocab_page.py. Do not edit this file directly. -->
```

## 手动更新英语单词页内容

如果只是手动修改已有英语单词页的内容，包括但不限于修改单词、中文译文、音标、单元名称、增删词条，优先改对应的 JSON 源数据，不要直接改 `subjects/english/.../words/index.html`。

### 1. 找到对应 JSON

当前已纳入框架的词汇页对应关系：

```text
三年级下学期单词：content/english/vocab/grade-3-second.json
四年级上学期单词：content/english/vocab/grade-4-first.json
五年级下学期单词：content/english/vocab/grade-5-second.json
```

如果不确定目标文件，可以先看 `site-manifest.json` 中对应页面的 `source` 字段。

### 2. 修改词条内容

每个词条在 `units[].items[]` 里面，结构固定为：

```json
{
  "english": "beautiful",
  "chinese": "美丽的",
  "phonetic": "/ˈbjuːtɪfl/"
}
```

常见改法：

- 修改单词或短语：改 `english`。
- 修改中文译文：改 `chinese`。
- 修改音标：改 `phonetic`。
- 新增词条：在目标 Unit 的 `items` 数组中新增一组同样结构的对象。
- 删除词条：从目标 Unit 的 `items` 数组中删除对应对象。
- 调整词条顺序：在同一个 `items` 数组内移动对象顺序。

注意：

- 三个字段都必须保留且非空。
- `english` 是朗读按钮的朗读文本来源，只放英文、短语或句型，不要混入中文。
- 如果是句型或带空位的表达，可以保留 `...`，例如 `I think you can ...`。
- `phonetic` 建议继续使用同一页已有风格，优先保持 `/.../` 或原页已有格式一致。
- 如果你只是不确定音标，先留给 Agent 根据 `english-vocabulary-html` skill 复核，不要随意填一个不确定的音标。

### 3. 修改单元名称

单元信息在 `units[]` 里面：

```json
{
  "id": "unit-1",
  "label": "Unit 1",
  "title": "Unit 1",
  "items": []
}
```

字段含义：

- `id`：页内锚点，通常不要改；例如 `unit-1`、`review`。
- `label`：单元导航和单元头部前缀；例如 `Unit 1`、`Review`。
- `title`：页面上显示的单元标题；如果要把 `Unit 1` 改成具体课题名，通常改这里。

如果只是想把页面上显示的单元名称改得更清楚，优先改 `title`。只有确实要改变导航显示时，才改 `label`。不要随意改 `id`，否则页内链接和校验可能失败。

### 4. 修改页面标题或统计文案

页面顶部文案在 JSON 顶层：

- `title`：页面主标题。
- `page_title`：浏览器标题。
- `description`：页面 meta 描述。
- `kicker`：标题上方的小标签。
- `summary`：主标题下方说明。
- `stats.unit_label`：左侧统计，例如 `8 个 Unit`。
- `stats.item_suffix`：总词条统计后缀，例如 `条`、`个词汇`。
- `unit_item_suffix`：每个 Unit 头部的数量后缀，例如 `条`、`个词汇/短语`。

普通修改词条时不需要改这些字段；新增或删除 Unit、调整页面范围时才需要同步检查。

### 5. 重新生成页面

在 `primary-knowledge-site` 目录运行：

```powershell
python tools/build_vocab_page.py --all
python tools/build_indexes.py
python tools/validate_site.py
```

这三条命令的作用：

- `build_vocab_page.py --all`：从 JSON 重新生成所有英语单词页 HTML。
- `build_indexes.py`：从 `site-manifest.json` 重新生成英语二级清单页。
- `validate_site.py`：检查 manifest、HTML 路径、Unit 数、词汇行数、朗读按钮数和听写标记。

如果只改了词条内容，没有新增或删除页面，通常 `site-manifest.json` 不需要改，但运行 `build_indexes.py` 没有坏处。

### 6. 本地预览

在 `primary-knowledge-site` 目录运行：

```powershell
python -m http.server 8080
```

打开目标页面检查显示效果：

```text
http://127.0.0.1:8080/subjects/english/grade-4/first/words/
```

检查重点：

- 修改后的单词、译文、音标是否正确。
- Unit 名称和导航是否正确。
- 朗读按钮仍能触发朗读。
- 听写模式仍能点击词条加入听写区。

### 7. 什么时候需要同步其它文件

只修改已有词条、译文、音标、单元标题时，通常只需要：

- 修改 `content/english/vocab/*.json`。
- 重新生成页面。
- 运行校验。

如果新增或删除整个页面，才需要同步：

- `site-manifest.json`
- `README.md`
- `SITE_MAINTENANCE.md`
- 必要时更新首页或其它清单页

如果只是小范围修正现有词条，可以在 `SITE_MAINTENANCE.md` 的变更记录中简短记录；如果只是临时自用的小改动，也可以根据实际需要决定是否记录。

## 常用命令

在 `primary-knowledge-site` 目录下执行：

```powershell
python tools/build_vocab_page.py --all
python tools/build_indexes.py
python tools/validate_site.py
python -m http.server 8080
```

然后访问：

```text
http://127.0.0.1:8080/
```

## JSON 基本结构

词汇页 JSON 的核心字段：

```json
{
  "schema": "english-vocab-page.v1",
  "id": "english-grade-4-first-words",
  "route": "/subjects/english/grade-4/first/words/",
  "output": "subjects/english/grade-4/first/words/index.html",
  "title": "广州英语四年级上册单词词汇",
  "page_title": "四年级上学期英语单词 | 小学知识点",
  "description": "广州英语四年级上册 Unit 1-8 与 Review 单词词汇、短语和句型",
  "kicker": "四年级 · 上学期 · 单词",
  "summary": "Unit 1-8 + Review | 单词、短语和句型合并整理 | 支持逐词朗读",
  "stats": {
    "unit_label": "8 个 Unit",
    "item_suffix": "条"
  },
  "unit_item_suffix": "条",
  "units": [
    {
      "id": "unit-1",
      "label": "Unit 1",
      "title": "Unit 1",
      "items": [
        {
          "english": "beautiful",
          "chinese": "美丽的",
          "phonetic": "/ˈbjuːtɪfl/"
        }
      ]
    }
  ]
}
```

约定：

- 每条词汇必须有 `english`、`chinese`、`phonetic`。
- 单词、短语、固定表达、句型都放入 `items`。
- `label` 用于单元导航和单元头部，例如 `Unit 1` 或 `Review`。
- `title` 用于单元标题。
- `id` 必须与页内锚点一致，例如 `unit-1`、`review`。

## Manifest 作用

`site-manifest.json` 记录站点页面清单和清单页卡片信息。第一阶段主要用于生成英语二级清单页。

新增英语单词页时，通常需要：

1. 新增 `content/english/vocab/*.json`。
2. 在 `site-manifest.json` 增加对应 `english-vocab` 页面。
3. 运行 `python tools/build_vocab_page.py --all`。
4. 运行 `python tools/build_indexes.py`。
5. 运行 `python tools/validate_site.py`。
6. 更新 `README.md` 和 `SITE_MAINTENANCE.md` 的当前页面与变更记录。

## 迁移说明

`tools/import_vocab_from_html.py` 是迁移辅助工具，用来把已有词汇页 HTML 抽取成 JSON。迁移完成后，日常维护不应依赖它；日常维护应直接改 JSON。

## 边界

- 本框架不替代已有 `english-vocabulary-html` skill；skill 仍用于整理来源材料、补齐中文和音标、判断内容质量。
- 本框架不负责从截图、PDF 或 Markdown 自动识别词汇；这些仍由 skill 或 Agent 先整理成 JSON。
- 不要把顶部导航、站点样式、朗读逻辑复制进生成页；继续复用 `assets/js/site-shell.js`、`assets/js/textbook-reader.js` 和 `assets/css/site.css`。
